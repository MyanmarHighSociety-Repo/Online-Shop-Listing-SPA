export interface Township {
    id: number;
    name: string;
    townList: [];
}

export interface TownshipOptions {
    id: number;
    value: string;
    selected: boolean;
// tslint:disable-next-line: eofline
}