export interface City {
    id: number;
    name: string;
    checked: boolean;
}

export interface CityOptions {
    id: number;
    value: string;
    selected: boolean;
// tslint:disable-next-line: eofline
}